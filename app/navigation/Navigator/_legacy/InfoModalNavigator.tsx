/**
 * InfoModalNavigator — Legacy. Stubbed for MVP.
 */
import React from "react";
import { View } from "react-native";

const InfoModalNavigator = (): React.ReactElement => <View />;

export default InfoModalNavigator;
