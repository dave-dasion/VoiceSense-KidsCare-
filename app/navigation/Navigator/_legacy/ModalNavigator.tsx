/**
 * ModalNavigator — Legacy (not used in MVP flow).
 * Stubbed to prevent bundling errors.
 */
import React from "react";
import { View } from "react-native";

const ModalNavigator = (): React.ReactElement => {
  return <View />;
};

export default ModalNavigator;
