/**
 * PeriodicityModalNavigator — Legacy. Stubbed for MVP.
 */
import React from "react";
import { View } from "react-native";

const PeriodicityModalNavigator = (): React.ReactElement => <View />;

export default PeriodicityModalNavigator;
